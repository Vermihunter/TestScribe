
function parse_template(node: Parser.SyntaxNode): Parameter {
    let param: Parameter;

    node.children.forEach(child=> {
        switch(child.type) {
            
        }
    });

    return param;
}