
TEST({{TestSuiteName}}Test, {{TestNameThrow}}) {
    // auto param = GetParam();
    EXPECT_TRUE(false);
    // EXPECT_THROW(func_call(), {{ExceptionType}});
}

TEST({{TestSuiteName}}Test, {{TestNameNoThrow}}) {
    // auto param = GetParam();
    EXPECT_TRUE(false);
    // EXPECT_NO_THROW(func_call(), {{ExceptionType}});
}
